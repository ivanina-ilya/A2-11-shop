import { MouseOverHighlightDirective } from './mouse-over-highlight.directive';

describe('MouseOverHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseOverHighlightDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
